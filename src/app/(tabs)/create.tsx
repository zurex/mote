import { Text } from 'react-native';

/**
 * DO NOT DELETE THIS PLACEHOLDER
 * It's hacky, but it's necessary to keep the file in the repo.
 * @returns result
 */
export default function CreateScreen() {
    return <Text>new.tsx</Text>;
}